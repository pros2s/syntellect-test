import { AlertControl } from 'models/AlertControl';
import { TextControl } from 'models/TextControl';
import './App.scss';

const App = () => (
  <div className='App'>
    <TextControl />
    <AlertControl />
  </div>
);

export default App;
